# dataroom-ai
AI-powered closing data room generator. Reads a term sheet, extracts the deal profile (deal type, assets, borrowers, guarantors), and builds a standardized folder structure with an expected-documents checklist. Drop in a messy pile of docs — it files each one, flags what's missing, and drafts chaser emails. Models do the busywork; lawyers audit.
